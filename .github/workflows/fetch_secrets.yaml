name: List Environment Secrets

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  list_secrets:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v3
      - name: List Environment Secrets
        run: |
          sh -x script.sh